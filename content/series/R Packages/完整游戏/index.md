---
title: "完整游戏"
date: 2026-03-23T12:36:20+08:00
lastmod: 2026-03-24T14:09:17+08:00
comments: true
math: false
weight: 1
tags:
    - R Packages
---

<!--more-->

## 初始化 R 包：`create_package()` 和 `use_git()`

可以直接使用命令：

```bash
R -e 'usethis::create_package("./src/regexcite")'
cd ./src/regexcite

R -e 'usethis::use_git()'
#> ✔ Setting active project to "/home/qihuan/Documents/RPackages/src/regexcite".
#> ✔ Initialising Git repo.
#> ✔ Adding ".Rproj.user", ".Rhistory", ".RData", ".httr-oauth", ".DS_Store", and ".quarto" to .gitignore.
```

`use_git()` 会添加 `.git` 和 `.gitignore`。在 RStudio 中使用 `create_package()` 则会自动调用 `use_git()`，打开一个新的 session 并额外创建额外的 `.Rbuildignore` `*.Rproj` 等文件。

```r
usethis::create_package("~/Documents/RPackages/src/regexcite")
# options(usethis.create_package.git = TRUE) # 默认，自动调用 use_git()
```


> [!WARNING]
> `devtools` 提供的 `create()` 函数也是用来初始化一个包，但是做了许多额外的事，导致**不透明**、**不可控**，因此不推荐使用。

## 添加函数：`use_r()`

使用 `use_r("strsplit1")` 创建并打开 `R/strsplit1.R` 文件，输入函数定义：

```r
strsplit1 <- function(x, split) {
  strsplit(x, split = split)[[1]]
}
```

## 模拟安装：`load_all()`

调用 `load_all()` 函数可以模拟包的安装和加载过程，但不会把函数加载到全局环境：

```r
load_all()
#> ℹ Loading regexcite

exists("strsplit1", where = globalenv(), inherits = FALSE)
#> FALSE
```

## 检查：`check()`

`check()` 相当于 `R CMD check` 命令。注意到现在有提示：

```r
check()
```

    ── R CMD check results ──────── regexcite 0.0.0.9000 ────
    Duration: 38.2s

    ❯ checking DESCRIPTION meta-information ... NOTE
    非标准执照规格
        `use_mit_license()`, `use_gpl3_license()` or friends to pick a
        license
    可以标准化：FALSE

    0 errors ✔ | 0 warnings ✔ | 1 note ✖

## 修改 `DESCRIPTION` 文件

示例：

    Package: regexcite
    Title: Make Regular Expressions More Exciting
    Version: 0.0.0.9000
    Authors@R:
        person("Jane", "Doe", , "jane@example.com", role = c("aut", "cre"))
    Description: Convenience functions to make some common tasks with string
        manipulation and regular expressions a bit easier.
    License: `use_mit_license()`, `use_gpl3_license()` or friends to pick a
        license
    Encoding: UTF-8
    Roxygen: list(markdown = TRUE)
    RoxygenNote: 7.1.2

## 添加 `LICENSE`：`use_*_license()`

这里使用 MIT 许可证：

```r
use_mit_license()
```

    ✔ Setting active project to "/home/qihuan/Documents/RPackages/src/regexcite".
    ✔ Adding "MIT + file LICENSE" to License.
    ✔ Writing LICENSE.
    ✔ Writing LICENSE.md.

再次 `check()`，可以发现前面的提示消失了。

## 为函数添加文档：`document()`

在函数定义的上方插入特殊注释，然后让 `roxygen2` 包来自动创建帮助文档。

> [!TIP]
> 在 RStudio 中，使用 <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>Alt</kbd>+<kbd>R</kbd> 快捷键快速为当前函数插入 Roxygen 注释。

> [!TIP]
> <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>P</kbd> 打开提示面板查询操作或快捷键，比如输入 “roxygen comment”。

示例如下：

```r
#' Split a string
#'
#' @param x A character vector with one element.
#' @param split What to split on.
#'
#' @return A character vector.
#' @export
#'
#' @examples
#' x <- "alfa,bravo,charlie,delta"
#' strsplit1(x, split = ",")
strsplit1 <- function(x, split) {
    strsplit(x, split = split)[[1]]
}
```

然后再调用

```r
document()
```

    ℹ Updating regexcite documentation
    ℹ Loading regexcite
    Writing NAMESPACE
    Writing strsplit1.Rd

`document()` 会修改 `NAMESPACE` 文件并创建 `man/strsplit1.Rd`（类似 LaTeX 格式）。

`NAMESPACE` 文件：

```r
# Generated by roxygen2: do not edit by hand

export(strsplit1)
```

> [!NOTE]
> 只有在 `NAMESPACE` 文件中 `export()` 的函数才能在安装并加载之后使用。

`man/strsplit1.Rd` 文件：

```latex
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strsplit1.R
\name{strsplit1}
\alias{strsplit1}
\title{Split a string}
\usage{
strsplit1(x, split)
}
\arguments{
\item{x}{A character vector with one element.}

\item{split}{What to split on.}
}
\value{
A character vector.
}
\description{
Split a string
}
\examples{
x <- "alfa,bravo,charlie,delta"
strsplit1(x, split = ",")
}
```

RStudio 能解析 *.Rd 文件并渲染成平常看到的帮助界面。

## 安装：`install()`

使用 `install()` 将包安装到 R 的全局 library 中：

```r
check() # better check again
install()
```


## 单元测试：`use_testthat()`

首先，使用 `use_testthat()` 函数初始化单元测试环境：

```r
use_testthat()
```

    ✔ Setting active project to "/home/qihuan/Documents/RPackages/src/regexcite".
    ✔ Adding testthat to Suggests field in DESCRIPTION.
    ✔ Adding "3" to Config/testthat/edition.
    ✔ Creating tests/testthat/.
    ✔ Writing tests/testthat.R.
    ☐ Call usethis::use_test() to initialize a basic test file and open it for editing.

当 `R/strsplit1.R` 是当前 RStudio 的活动窗口时，不用参数直接调用 `use_test()` 会自动打开或者创建对应的测试文件：

```r
ues_test()
```

    ✔ Writing tests/testthat/test-strsplit1.R.
    ☐ Modify tests/testthat/test-strsplit1.R.

替换为以下内容：

```r
test_that("strsplit1() splits a string", {
    expect_equal(strsplit1("a,b,c", split = ","), c("a", "b", "c"))
})
```

接下来就可以使用 `test()` 函数来自动运行包中的所有测试：

```r
test()
```

    ==> devtools::test()

    ℹ Testing regexcite
    ✔ | F W  S  OK | Context
    ✔ |          1 | strsplit1

    ══ Results ══════════════════════════
    [ FAIL 0 | WARN 0 | SKIP 0 | PASS 1 ]

> [!TIP]
> RStudio 中可以使用快捷键 <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>T</kbd> 来在“Build”面板调用 `test()` 函数。

## 声明依赖：`use_package()`

为当前包声明 `stringr` 依赖：

```r
use_package("stringr")
```
    ✔ Adding stringr to Imports field in DESCRIPTION.
    ☐ Refer to functions with `stringr::fun()`.

修改 `strsplit1()` 让其更符合 `stringr` 风格：

```r
str_split_one <- function(string, pattern, n = Inf) {
    stopifnot(is.character(string), length(string) <= 1) if (length(string) == 1) {
        stringr::str_split(string = string, pattern = pattern, n = n)[[1]]
    } else {
        character()
    }
}
```

## 修改文件名：`rename_files()`

使用 `rename_files()` 修改所有相关文件名：

```r
rename_files("strsplit1", "str_split_one")
```

    ✔ Moving R/strsplit1.R to R/str_split_one.R.
    ✔ Moving tests/testthat/test-strsplit1.R to tests/testthat/test-str_split_one.R.

修改文件名之后，还需要**手动修改文件内容**。

```r
#' Split a string
#'
#' @param string A character vector with, at most, one element.
#' @inheritParams stringr::str_split
#'
#' @return A character vector.
#' @export
#'
#' @examples
#' x <- "alfa,bravo,charlie,delta"
#' str_split_one(x, pattern = ",")
#' str_split_one(x, pattern = ",", n = 2)
#'
#' y <- "192.168.0.1"
#' str_split_one(y, pattern = stringr::fixed("."))
str_split_one <- function(string, pattern, n = Inf) {
    stopifnot(is.character(string), length(string) <= 1)
    if (length(string) == 1) {
        stringr::str_split(string = string, pattern = pattern, n = n)[[1]]
    } else {
        character()
    }
}
```

```r
test_that("str_split_one() splits a string", {
    expect_equal(str_split_one("a,b,c", ","), c("a", "b", "c"))
})

test_that("str_split_one() errors if input length > 1", {
    expect_error(str_split_one(c("a,b","c,d"), ","))
})

test_that("str_split_one() exposes features of stringr::str_split()", {
    expect_equal(str_split_one("a,b,c", ",", n = 2), c("a", "b,c"))
    expect_equal(str_split_one("a.b", stringr::fixed(".")), c("a", "b"))
})
```

此外，还需要调用 `document()` 重新生成函数文档以及更新 `NAMESPACE`：

```r
document()
```

    ℹ Updating regexcite documentation
    ℹ Loading regexcite
    Writing NAMESPACE
    Writing str_split_one.Rd
    Deleting strsplit1.Rd

    警告信息:
    Objects listed as exports, but not present in namespace:
    • strsplit1

## 连接到 GitHub：`use_github()`

这里不做讨论。

## 软件包的门面：`use_readme_rmd()`

使用 `use_readme_rmd()` 创建一个模板 `README.Rmd` 文件：

```r
use_readme_rmd()
```

    ✔ Writing README.Rmd.
    ✔ Adding "^README\\.Rmd$" to .Rbuildignore.
    ☐ Modify README.Rmd.
    ☐ Update README.Rmd to include installation instructions.
    ✔ Writing .git/hooks/pre-commit.

注意到写入了 `.git/hooks/pre-commit` 文件，这里创建了一个提交前的检查，用来检查 `README.md` 是否需要重新生成（re-knit）。

现在来编辑 `README.Rmd`，示例内容如下：

````rmd
---
output: github_document
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "man/figures/README-",
  out.width = "100%"
)
```

**NOTE: This is a toy package created for expository purposes, for the second edition of [R Packages](https://r-pkgs.org). It is not meant to actually be useful. If you want a package for factor handling, please see [stringr](https://stringr.tidyverse.org), [stringi](https://stringi.gagolewski.com/),
[rex](https://cran.r-project.org/package=rex), and
[rematch2](https://cran.r-project.org/package=rematch2).**

# regexcite

<!-- badges: start -->
<!-- badges: end -->

The goal of regexcite is to make regular expressions more exciting!
It provides convenience functions to make some common tasks with string manipulation and regular expressions a bit easier.

## Installation

You can install the development version of regexcite from [GitHub](https://github.com/) with:
      
``` r
# install.packages("devtools")
devtools::install_github("jennybc/regexcite")
```

## Usage

A fairly common task when dealing with strings is the need to split a single string into many parts.
This is what `base::strplit()` and `stringr::str_split()` do.

```{r}
(x <- "alfa,bravo,charlie,delta")
strsplit(x, split = ",")
stringr::str_split(x, pattern = ",")
```

Notice how the return value is a **list** of length one, where the first element holds the character vector of parts.
Often the shape of this output is inconvenient, i.e. we want the un-listed version.

That's exactly what `regexcite::str_split_one()` does.

```{r}
library(regexcite)

str_split_one(x, pattern = ",")
```

Use `str_split_one()` when the input is known to be a single string.
For safety, it will error if its input has length greater than one.

`str_split_one()` is built on `stringr::str_split()`, so you can use its `n` argument and stringr's general interface for describing the `pattern` to be matched.

```{r}
str_split_one(x, pattern = ",", n = 2)

y <- "192.168.0.1"
str_split_one(y, pattern = stringr::fixed("."))
```
````


然后使用 `build_readme()` 来创建 `README.md`：

```r
build_readme()
```

    ℹ Installing regexcite in temporary library
    ℹ Building /home/qihuan/Documents/RPackages/src/regexcite/README.Rmd


🎉🎉🎉恭喜！干得不错！现在整个流程都已经结束了，最后再运行一次 `check()` 和 `install()` 即可，遇到任何问题可以与作者部署在 GitHub 上的[完整示例包](https://github.com/jennybc/regexcite)对比。
